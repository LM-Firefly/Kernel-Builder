Place optional mihomo patch files in a channel directory matching `kernel-builder.json`.

Patch files are applied after the template repository's own patches. Keep patches small and make
them apply cleanly to the selected upstream ref.
